	<canvas  data-total='{{ $total }}' data-filled="{{ $fill }}"  height="{{ $width }}" width="{{ $width }}" id="{{ $id}}" ></canvas>
	<div class="canvas-overlay"><strong>{{ $fill }}</strong>/{{ $total }}</div>
	<div class="donut-label">{{ $label }}</div>
