

<div class="formitem clearfix">
	<?php if(isset($label)): ?>
		<div class="formlabel">
			<?php echo Form::label( $fieldname, $label); ?>
		</div>
	<?php endif; ?>
	<div class="formfield">

		<div id="categoryField">
		  <input class="category" placeholder="<?php echo trans('discover.search_box'); ?>" type="text" name="<?php echo $fieldname; ?>" id="<?php echo $fieldname; ?>" value="<?php echo isset($selectedCategory) ? $selectedCategory : ''; ?>"/>
		</div>
	</div>
</div>
