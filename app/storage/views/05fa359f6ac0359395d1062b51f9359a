<header class="navigation">
	<section>
		<nav>		
			<ul>
				<a class="evercise-logo" href="<?php echo URL::route('home', null); ?>"><?php echo HTML::image('/img/evercise logo yellow.png', 'evercise logo', array('class' => 'logo'));; ?></a>

        <?php echo HTML::linkRoute('evercisegroups.search', trans('header.discover_classes') , null , ['class' => Route::currentRouteName() == 'evercisegroups.search' ? 'active' : '']); ?>
        <?php echo HTML::linkRoute('static.how_it_works', trans('header.how_it_works') , null , ['class' => Route::currentRouteName() == 'static.how_it_works' ? 'active' : '']); ?>

        <?php if(isset($user)): ?>
          <div class="nav-end">
            <?php if ($user->inGroup($trainerGroup)): ?>
              <?php echo HTML::linkRoute('evercisegroups.index', 'Class Hub' , null , ['class' => Route::currentRouteName() == 'evercisegroups.index' ? 'active' : '']); ?>
              <div class="user-box">
                <a  class = "<?php echo Route::currentRouteName() == 'trainers.edit' ? 'active' : null; ?>"  href="<?php echo URL::route( 'trainers.edit', $user->id ); ?>"><?php echo HTML::image( $userImage, $user->display_name.'s image'); ?> <span><?php echo $user->display_name; ?></span></a>
              </div>
            <?php else: ?>
              <div class="user-box">
                <a  class  = "<?php echo Route::currentRouteName() == 'users.edit' ? 'active' : null; ?>" href="<?php echo URL::route( 'users.edit', $user->id ); ?>"><?php echo HTML::image( $userImage, $user->display_name.'s image');; ?> <span><?php echo $user->display_name; ?></span></a>
              </div>

            <?php endif; ?>
          </div>
        <?php else: ?>
          <div class="nav-end">
             
             <?php if(isset($redirect_after_login)): ?>
              <?php echo HTML::link('/auth/login/'.Route::getCurrentRoute()->getName() , trans('header.login'),  array('id'=>'login', 'class' => 'login ')); ?>
              
            <?php else: ?>
              <?php echo HTML::linkRoute('auth.login', 'Login', null, array('id'=>'login', 'class' => 'login')); ?>
            <?php endif; ?>
            <?php echo HTML::linkRoute('users.create', trans('header.register') , null , [ 'class' =>  Route::currentRouteName() == 'users.create' ? 'active' : '']); ?>
          </div>

        <?php endif; ?>
        <?php if(isset($user)): ?>
         <?php if ($user->inGroup($adminGroup)): ?>
         <div class="nav-admin">
          <div id="displayName-dropdown" class="dropdown-menu">
          
              <?php echo HTML::linkRoute('admin.pending', 'pending trainers'); ?>
              <?php echo HTML::linkRoute('admin.pending_withdrawal', 'pending withdrawals'); ?>
            </div>
          </div>
         <?php endif; ?>
        <?php endif; ?>

         <?php /* <li><?php echo HTML::linkRoute('static.how_it_works', trans('header.how_it_works')); ?></li>

          <li></li>

          <?php if(isset($user)): ?>
              <?php if ($user->inGroup($trainerGroup)): ?>

                  <div class="nav-end">
                      <a  href="<?php echo URL::route( 'trainers.edit', $user->id ); ?>"><?php echo HTML::image( $userImage, $user->display_name.'s image', array('class'=> 'profile-pic'));; ?></a>
                      <li id="displayName">
                          <?php echo $user->display_name; ?>
                          <?php echo HTML::image( 'img/down-arrow.png', 'down-arrow', array('class'=> 'displayName-dropdown-arrow')); ?>
                      </li>
                      <div id="displayName-dropdown" class="dropdown-menu">
                           <span><?php echo HTML::linkRoute('trainers.edit', 'My Dashboard' , $user->id); ?></span>
                          <span><?php echo HTML::linkRoute('evercisegroups.index', 'Class Hub'); ?></span>

                          
                          <?php if ($user->inGroup($adminGroup)): ?>
                            <hr>
                            <span><?php echo HTML::linkRoute('admin.pending', 'pending trainers'); ?></span>
                            <span><?php echo HTML::linkRoute('admin.pending_withdrawal', 'pending withdrawals'); ?></span>
                          <?php endif; ?>
                      </div>
                  </div>
                  <div class="nav-join">
                    <div class="nav-login">
                      <li><?php echo HTML::linkRoute('evercisegroups.index', 'Class Hub'); ?></li>
                    </div>
                  </div>                    
             <?php else: ?>
                 <div class="nav-end">
                      <a  href="<?php echo URL::route( 'users.edit', $user->id ); ?>"><?php echo HTML::image( $userImage, $user->display_name.'s image', array('class'=> 'profile-pic'));; ?></a>
                     <li id="displayName">
                          <?php echo $user->display_name; ?>
                          <?php echo HTML::image( 'img/down-arrow.png', 'down-arrow', array('class'=> 'displayName-dropdown-arrow')); ?>
                     </li>
                     <div id="displayName-dropdown" class="dropdown-menu">
                           <span><?php echo HTML::linkRoute('users.edit', 'My Dashboard' , $user->id); ?></span>


                          <?php if ($user->inGroup($adminGroup)): ?>
                            <hr>
                            <span><?php echo HTML::linkRoute('admin.pending', 'pending trainers'); ?></span>
                            <span><?php echo HTML::linkRoute('admin.pending_withdrawal', 'pending withdrawals'); ?></span>
                          <?php endif; ?>
                     </div>
                 </div>
                 <div class="nav-join">
                 </div>
             <?php endif; ?>

          <?php else: ?>
              <div class="nav-end">
                 <li><?php echo HTML::linkRoute('users.create', trans('header.register')); ?></li>
              </div>
              <div class="nav-join">
                   <div class="nav-login">
                      <?php if(isset($redirect_after_login)): ?>
                         <li><?php echo HTML::link('/auth/login/'.Route::getCurrentRoute()->getName() , trans('header.login'),  array('id'=>'login', 'class' => 'login')); ?></li>
                        
                      <?php else: ?>
                         <li> <?php echo HTML::linkRoute('auth.login', 'Login', null, array('id'=>'login', 'class' => 'login')); ?></li>
                      <?php endif; ?>
                   </div>
                  
              </div>
          <?php endif; ?>
          */ ?>
      </ul>
		</nav>
	</section>
</header>